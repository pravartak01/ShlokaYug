/**
 * API Client Configuration
 * Re-export the main API client from config for consistency
 */

import api from '../config/api';

// Re-export the main API client - this ensures consistent token handling
export default api;
